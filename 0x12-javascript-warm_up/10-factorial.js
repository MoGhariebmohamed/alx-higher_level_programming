#!/usr/bin/node
function factorial (a) {
  a = process.argv[2];
  const sum = Number(a) + Number(b);
  if (isNaN(a)) {
    console.log(1);
  } else {
      for (let i = 0; i <= a; i--) {
        console.log(sum);
  }
}add();
